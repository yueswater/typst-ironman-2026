= 我的 Typst 筆記
== 今天學到的語法

Typst 是一個現代化的排版系統，語法比 *LaTeX* 簡潔許多，但一樣具備強大的排版能力，很適合拿來寫論文、履歷或報告。

=== 這篇學到的重點

- 標題語法：用 `=` 決定層級
- 文字樣式：`*粗體*` 與 `_斜體_`
- 清單語法：`-` 無序、`+` 有序

=== 下一步

+ 學會編譯指令
+ 練習更多基本語法
+ 挑戰排版一份履歷
